version: '3.7'

services:
  just-buy-shop:
    container_name: just-buy-shop
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '8081:8080'